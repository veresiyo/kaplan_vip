name: Flight Status Monitor

on:
    # Her 5 dakikada bir çalış (UTC saatinde)
    schedule:
        - cron: "*/5 * * * *"

    # Manual tetikleme
    workflow_dispatch:

jobs:
    update-flights:
        runs-on: ubuntu-latest
        timeout-minutes: 10

        steps:
            - name: 📥 Checkout code
              uses: actions/checkout@v4

            - name: 🐍 Set up Python 3.11
              uses: actions/setup-python@v5
              with:
                  python-version: "3.11"

            - name: 📦 Install dependencies
              run: |
                  python -m pip install --upgrade pip
                  pip install -r requirements.txt

            - name: 🔐 Decode Firebase Service Account Key
              run: |
                  mkdir -p scripts
                  # Secret'ı base64'ten decode et
                  echo "${{ secrets.FIREBASE_SERVICE_ACCOUNT_BASE64 }}" | base64 -d > scripts/serviceAccountKey.json
                  # Doğrulama: valid JSON mi?
                  python -c "import json; json.load(open('scripts/serviceAccountKey.json'))"

            - name: 🚀 Run Flight Scraper
              env:
                  FIREBASE_CREDENTIALS: scripts/serviceAccountKey.json
                  ENVIRONMENT: production
              run: |
                  python scripts/flight_scraper.py --once
                  echo "✅ Flight scraper başarıyla çalıştı"

            - name: 📊 Log Summary
              if: success()
              run: |
                  echo "✅ Workflow başarılı olarak tamamlandı"
                  echo "⏱️ Tarih: $(date -u)"
                  echo "🔥 Proje: kaplanvip-1fad8"

            - name: ⚠️ Error Notification
              if: failure()
              run: |
                  echo "❌ Workflow başarısız oldu"
                  echo "📝 Status: ${{ job.status }}"

            - name: 🧹 Cleanup
              if: always()
              run: |
                  rm -f scripts/serviceAccountKey.json 2>/dev/null || true